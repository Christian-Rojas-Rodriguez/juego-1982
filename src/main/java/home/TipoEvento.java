package home;

/**
 * Enum del HOME team — define los tipos de eventos que un módulo puede disparar.
 * No modificar: definido por el grupo Lobby (repo Modulo_1_Algoritmos_1).
 */
public enum TipoEvento {
    INICIADO,
    PAUSADO,
    REANUDADO,
    FINALIZADO,
    ERROR
}
